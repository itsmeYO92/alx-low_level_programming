#include <stdlib.h>
#include <string.h>

/**
 * argstostr - concatenat all the arguments
 * @ac: number of arguments
 * @av: arguments
 * Return: string or NULL
 */
char *argstostr(int ac, char **av)
{
	int i;

	if (ac == 0 || av == NULL)
		return (NULL)

	
	{
		for (i = 0; i < height; i++)
		{
			free(grid[i]);
		}

		free(grid);
	}
}
