#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: Print numbers from 10 to 9.
 *
 * Return: 0 if successful
*/
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}

